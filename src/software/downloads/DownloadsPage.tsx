import { useState } from "react";
import { Clock3, Download, FileArchive } from "lucide-react";
import { Button, Card, DataTable, FormField, MetricCard, SectionHeader, SelectInput, type DataTableColumn } from "../../shared/components";
import { softwareStore, type DownloadRecord } from "../data/softwareStore";
import { shortDate } from "../shared/format";
import { SoftwareStatus } from "../shared/SoftwareStatus";
import { useSoftwareStore } from "../shared/useSoftwareStore";

export function DownloadsPage(){ const rows=useSoftwareStore(softwareStore.getDownloads), licenses=useSoftwareStore(softwareStore.getLicenses), releases=useSoftwareStore(softwareStore.getReleases), products=useSoftwareStore(softwareStore.getProducts), settings=useSoftwareStore(softwareStore.getSettings); const [licenseId,setLicenseId]=useState(licenses[0]?.id??""); const eligibleReleases=releases.filter((r)=>r.status==="published"&&r.productId===licenses.find((l)=>l.id===licenseId)?.productId); const [releaseId,setReleaseId]=useState(eligibleReleases[0]?.id??""); const cols:DataTableColumn<DownloadRecord>[]=[{key:"file",header:"Download",render:(d)=><div className="entity-cell"><strong>{d.fileName}</strong><small>{products.find((p)=>p.id===d.productId)?.name} · {d.tokenHint}</small></div>},{key:"status",header:"Status",render:(d)=><SoftwareStatus value={new Date(d.expiresAt)<new Date()?"expired":d.status}/>},{key:"usage",header:"Attempts",render:(d)=><strong>{d.downloadCount} / {settings.maxDownloadAttempts}</strong>},{key:"expires",header:"Expires",render:(d)=><span>{shortDate(d.expiresAt)}</span>},{key:"action",header:"",render:(d)=><div className="table-actions">{d.status!=="expired"&&d.downloadCount<settings.maxDownloadAttempts?<Button variant="primary" onClick={()=>softwareStore.recordDownload(d.id)}><Download size={14}/> Record download</Button>:null}{d.status!=="expired"?<Button onClick={()=>softwareStore.expireDownload(d.id)}>Expire</Button>:null}</div>}]; return <div className="page"><SectionHeader eyebrow="NEXT F Software" title="Secure Downloads" description="Download records are entitlement-bound. Real signed R2 delivery is intentionally deferred to the infrastructure stage."/><div className="compact-metrics"><MetricCard label="Issued" value={String(rows.length)} detail="Download authorizations" icon={FileArchive}/><MetricCard label="Downloaded" value={String(rows.filter((d)=>d.downloadCount>0).length)} detail="Used authorizations" icon={Download}/><MetricCard label="Token window" value={`${settings.downloadTokenHours}h`} detail="Local policy" icon={Clock3}/></div><Card className="operation-section"><div className="operation-section__head"><div><span>Entitlement</span><h3>Issue download</h3></div><FileArchive size={18}/></div><div className="form-grid form-grid--two"><FormField label="License"><SelectInput value={licenseId} onChange={(e)=>{const id=e.target.value;setLicenseId(id);setReleaseId(releases.find((r)=>r.status==="published"&&r.productId===licenses.find((l)=>l.id===id)?.productId)?.id??"");}}>{licenses.filter((l)=>["active","grace"].includes(l.status)).map((l)=><option key={l.id} value={l.id}>{l.key}</option>)}</SelectInput></FormField><FormField label="Release"><SelectInput value={releaseId} onChange={(e)=>setReleaseId(e.target.value)}>{releases.filter((r)=>r.status==="published"&&r.productId===licenses.find((l)=>l.id===licenseId)?.productId).map((r)=><option key={r.id} value={r.id}>v{r.version} · {r.channel}</option>)}</SelectInput></FormField></div><div className="modal-actions"><Button variant="primary" onClick={()=>licenseId&&releaseId&&softwareStore.issueDownload(licenseId,releaseId)}>Issue authorization</Button></div></Card><Card><DataTable rows={rows} columns={cols} getKey={(d)=>d.id}/></Card></div>; }
