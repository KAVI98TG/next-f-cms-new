# CMS v1.0.18 — Canonical cleanup build hotfix

This hotfix corrects TypeScript compile regressions found during the first local production build of v1.0.17.

- Supplies required `state` props to canonical Customers and Gaming Dashboard `StatePanel` instances.
- Uses canonical `NextFGamingProduct.kind` merchandising categories for storefront hero-product search instead of a nonexistent `category` field.
- Contains all v1.0.17 canonical Gaming cleanup and Customer 360 changes unchanged otherwise.
