import fs from "node:fs";
import path from "node:path";
const root=process.cwd(), pass=[], fail=[]; const check=(name,ok)=>ok?pass.push(name):fail.push(name); const read=(file)=>fs.readFileSync(path.join(root,file),"utf8");
const required=[
  "src/app/version.ts","src/services/acceptance/finalAcceptance.ts","src/platform/acceptance/AcceptancePage.tsx","src/shared/utils/csv.ts","src/shared/components/ExportCsvButton.tsx"
];
for(const file of required)check(`file ${file}`,fs.existsSync(path.join(root,file)));
const pkg=JSON.parse(read("package.json")); const version=read("VERSION").trim(); const appVersion=read("src/app/version.ts"); const nav=read("src/app/navigation.ts"), app=read("src/app/App.tsx"), sidebar=read("src/app/layout/Sidebar.tsx"), acceptance=read("src/services/acceptance/finalAcceptance.ts"), acceptancePage=read("src/platform/acceptance/AcceptancePage.tsx"), csv=read("src/shared/utils/csv.ts"), exportButton=read("src/shared/components/ExportCsvButton.tsx"), css=read("src/css/components.css")+read("src/css/responsive.css");
check("package version is defined",/^\d+\.\d+\.\d+$/.test(pkg.version));
check("VERSION file matches package",version===pkg.version);
check("central app version matches package",appVersion.includes(`APP_VERSION = "${pkg.version}"`));
check("sidebar uses central version",sidebar.includes("APP_VERSION")&&!sidebar.includes("V0.7.0"));
check("acceptance npm script",pkg.scripts?.["check:acceptance"]==="node scripts/final-acceptance-check.mjs");
check("acceptance navigation",nav.includes('path: "/platform/acceptance"'));
check("acceptance route",app.includes('"/platform/acceptance": <AcceptancePage />'));
check("acceptance page rerun",acceptancePage.includes("Run checks")&&acceptancePage.includes("runFinalAcceptance"));
check("acceptance page filtering",acceptancePage.includes("All categories")&&acceptancePage.includes("All states"));
check("acceptance report export",acceptancePage.includes("Export report")&&acceptancePage.includes("downloadCsv"));
check("acceptance score",acceptancePage.includes("Acceptance score")&&acceptancePage.includes("productReady"));
check("acceptance responsive styles",css.includes(".acceptance-banner")&&css.includes(".acceptance-row")&&css.includes("max-width:680px"));
check("application route integrity check",acceptance.includes("Unique route registry")&&acceptance.includes("Workspace route boundaries"));
check("browser storage probe",acceptance.includes("storageAvailable")&&acceptance.includes("localStorage"));
check("platform user-role integrity",acceptance.includes("Staff role references"));
check("platform unique staff emails",acceptance.includes("Unique staff emails"));
check("platform permission catalog integrity",acceptance.includes("Known role permissions")&&acceptance.includes("Manage/read dependency"));
check("digital lead/service integrity",acceptance.includes("Lead → service references"));
check("digital opportunity integrity",acceptance.includes("Opportunity relationships"));
check("digital proposal integrity",acceptance.includes("Proposal relationships"));
check("digital invoice integrity",acceptance.includes("Invoice relationships")&&acceptance.includes("Invoice value integrity"));
check("digital project integrity",acceptance.includes("Project relationships")&&acceptance.includes("Project progress bounds"));
check("digital delivery integrity",acceptance.includes("Task/deliverable/approval relationships"));
check("digital recurring integrity",acceptance.includes("Recurring-service relationships"));
check("digital site/support integrity",acceptance.includes("Site/support relationships"));
check("gaming supplier catalog integrity",acceptance.includes("Supplier catalog references"));
check("gaming product mapping integrity",acceptance.includes("Storefront product mapping"));
check("gaming order integrity",acceptance.includes("Order relationships")&&acceptance.includes("Unique order idempotency keys"));
check("gaming economics integrity",acceptance.includes("Order economics")&&acceptance.includes("Minimum-profit safeguard"));
check("gaming support integrity",acceptance.includes("Gaming support relationships"));
check("software edition/release integrity",acceptance.includes("Edition → product references")&&acceptance.includes("Release → product references"));
check("software order/license integrity",acceptance.includes("Software order relationships")&&acceptance.includes("License relationships"));
check("software license uniqueness",acceptance.includes("Unique license keys"));
check("software activation limits",acceptance.includes("Activation limits"));
check("software subscription integrity",acceptance.includes("Software subscription relationships"));
check("software download integrity",acceptance.includes("Download authorization relationships"));
check("software support/update integrity",acceptance.includes("Software support relationships")&&acceptance.includes("Update-check relationships"));
check("shared account integrity",acceptance.includes("Normalized account identities"));
check("shared payment integrity",acceptance.includes("Unified payment values"));
check("shared search/operations integrity",acceptance.includes("Global search index")&&acceptance.includes("Operations inbox"));
check("infrastructure remains deferred",acceptance.includes("Production infrastructure")&&acceptance.includes('status:"deferred"'));
check("CSV escaping",csv.includes("replaceAll")&&csv.includes("createCsv"));
check("CSV browser download",csv.includes("URL.createObjectURL")&&csv.includes("anchor.download"));
check("shared CSV action feedback",exportButton.includes("useToast")&&exportButton.includes("CSV exported"));
for(const [name,file] of [["shared account export","src/platform/accounts/AccountsPage.tsx"],["unified payment export","src/platform/payments/PaymentsPage.tsx"],["digital report export","src/next-f/reports/ReportsPage.tsx"],["gaming finance export","src/gaming-store/finance/FinancePage.tsx"],["software analytics export","src/software/analytics/AnalyticsPage.tsx"]]) check(name,read(file).includes("ExportCsvButton"));
const services=read("src/next-f/services/ServicesPage.tsx"), sites=read("src/next-f/sites/SitesPage.tsx"), suppliers=read("src/gaming-store/suppliers/SuppliersPage.tsx"), swProducts=read("src/software/products/ProductsPage.tsx");
check("digital service validation",services.includes("validators.positive")&&services.includes("already exists")&&services.includes("useToast"));
check("digital site URL/domain validation",sites.includes("validators.url")&&sites.includes("validDomain")&&sites.includes("aria-invalid"));
check("digital maintenance guarded",sites.includes("ConfirmDialog")&&sites.includes("Record maintenance mode"));
check("supplier key validation",suppliers.includes("validators.slug")&&suppliers.includes("already registered"));
check("supplier disable guarded",suppliers.includes("ConfirmDialog")&&suppliers.includes("Disable supplier"));
check("software product slug validation",swProducts.includes("validators.slug")&&swProducts.includes("slug already exists"));
check("software edition value validation",swProducts.includes("validators.positive")&&swProducts.includes("Activation limit"));
check("software product feedback",swProducts.includes("useToast")&&swProducts.includes("Software product created"));
const source=[]; const walk=(dir)=>{for(const e of fs.readdirSync(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())walk(f);else if(/\.(ts|tsx)$/.test(e.name))source.push(f);}}; walk(path.join(root,"src"));
let broken=0; for(const file of source){const text=fs.readFileSync(file,"utf8");for(const m of text.matchAll(/from\s+["'](\.[^"']+)["']/g)){const base=path.resolve(path.dirname(file),m[1]);if(![base,`${base}.ts`,`${base}.tsx`,path.join(base,"index.ts"),path.join(base,"index.tsx")].some(fs.existsSync))broken++;}}
check(`${source.length} TS/TSX source files discovered`,source.length>=110); check("relative imports resolve",broken===0);
const forbidden=[]; const scan=(dir)=>{for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(["node_modules","dist"].includes(e.name))continue;const f=path.join(dir,e.name);if(e.isDirectory())scan(f);else if(e.name===".gitkeep"||e.name==="README.md"&&f.includes(`${path.sep}src${path.sep}`))forbidden.push(f);}}; scan(path.join(root,"src")); check("no placeholder architecture",forbidden.length===0);
check("no Cloudflare runtime/config",!["wrangler.toml","wrangler.jsonc","worker"].some((name)=>fs.existsSync(path.join(root,name))));
const todo=source.flatMap((file)=>{const text=fs.readFileSync(file,"utf8");return /\b(TODO|FIXME)\b/.test(text)?[file]:[];}); check("no TODO/FIXME debt in implementation",todo.length===0);
console.log("NEXT F CMS Final Product Acceptance regression check"); for(const item of pass)console.log(`PASS  ${item}`); for(const item of fail)console.error(`FAIL  ${item}`); console.log(`\n${pass.length} passed, ${fail.length} failed`); if(fail.length)process.exit(1);
